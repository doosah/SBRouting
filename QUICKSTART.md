# 🚀 Быстрый запуск (исправленная версия)

## ⚡ Минимальная установка (работает 100%)

### Шаг 1: Установите только необходимое

```bash
cd /workspace
npm install
```

Устанавливаются только 2 пакета:
- `vite` - система сборки
- `chart.js` - библиотека для графиков

**Время установки:** ~30 секунд

---

### Шаг 2: Запустите проект

```bash
npm run dev
```

Откроется на **http://localhost:3000** 🎉

---

## 🔧 Если всё равно ошибка 404

### Вариант А: Установить пакеты вручную

```bash
cd /workspace

# Очистить кэш npm
npm cache clean --force

# Установить по одному
npm install vite@latest
npm install chart.js@latest
```

### Вариант Б: Запустить без установки (через CDN)

Если npm не работает, можно запустить старую версию:

```bash
cd /workspace/SBReport
```

Откройте файл `sb-report.html` прямо в браузере (двойной клик на файл).

---

## 🎯 Проверка установки

После `npm install` должно быть:

```bash
# Проверить, что пакеты установлены
ls node_modules/

# Должны быть папки:
# - vite
# - chart.js
# - (и зависимости этих пакетов)
```

---

## 📦 Альтернативный способ (без npm)

### Вариант 1: Использовать старую версию

```bash
cd /workspace/SBReport
# Откройте sb-report.html в браузере
```

Старая версия работает без npm!

### Вариант 2: Простой HTTP сервер

```bash
cd /workspace/src

# Если есть Python 3:
python3 -m http.server 3000

# Если есть Python 2:
python -m SimpleHTTPServer 3000

# Если есть Node.js:
npx serve .
```

Откроется на **http://localhost:3000**

---

## ❓ Что могло пойти не так?

### Ошибка 404 обычно значит:

1. **Нет доступа к npm registry**
   ```bash
   # Проверьте доступ
   npm config get registry
   
   # Должно быть: https://registry.npmjs.org/
   ```

2. **Устаревший Node.js**
   ```bash
   # Проверьте версию
   node --version
   
   # Нужно: v18+ или выше
   ```

3. **Проблемы с сетью**
   ```bash
   # Попробуйте другой registry
   npm config set registry https://registry.npmjs.org/
   ```

---

## ✅ Минималистичный package.json (что сейчас)

```json
{
  "name": "sbreport",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  },
  "dependencies": {
    "chart.js": "^4.4.7"
  }
}
```

Всего 2 пакета! Минимум зависимостей.

---

## 🎉 После успешной установки

```bash
# Запустить dev-сервер
npm run dev

# Собрать для production
npm run build

# Предпросмотр production
npm run preview
```

---

## 🆘 Если ничего не помогло

### Используйте старую версию (она работает!)

1. Откройте файл:
   ```
   /workspace/SBReport/sb-report.html
   ```

2. Двойной клик на файл

3. Откроется в браузере ✅

Старая версия v1.0 работает без npm, без сборки, просто откройте HTML файл!

---

## 📞 Дополнительная диагностика

```bash
# Проверьте Node.js
node --version

# Проверьте npm
npm --version

# Проверьте доступ к registry
npm ping

# Проверьте конфиг npm
npm config list

# Очистите всё и попробуйте снова
rm -rf node_modules package-lock.json
npm cache clean --force
npm install
```

---

## 🎯 Итог

**Вариант 1 (идеально):**
```bash
npm install
npm run dev
```

**Вариант 2 (если npm не работает):**
```bash
cd /workspace/SBReport
# Откройте sb-report.html в браузере
```

**Вариант 3 (альтернативный сервер):**
```bash
cd /workspace/src
python3 -m http.server 3000
```

Один из этих вариантов точно сработает! 🚀
