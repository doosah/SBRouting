# 🚀 Автоматический деплой на GitHub Pages

## Как это работает

1. При каждом пуше в ветку `main` автоматически запускается деплой
2. GitHub Actions собирает проект (`npm run build`)
3. Результат публикуется на GitHub Pages
4. Приложение доступно по адресу: **https://doosah.github.io/SBRouting/**

## Первичная настройка (нужно сделать ОДИН раз)

### В репозитории GitHub:

1. Перейдите в: **Settings** → **Pages**
2. В разделе **Source** выберите: **GitHub Actions**
3. Сохраните

Всё! После этого каждый пуш в `main` будет автоматически деплоить сайт.

## Статус деплоя

После пуша вы увидите:
- В разделе **Actions** - прогресс сборки
- Зеленая галочка ✅ = деплой успешен
- Красный крестик ❌ = ошибка (см. логи)

## Ссылка на приложение

После успешного деплоя приложение будет доступно:

**🌐 https://doosah.github.io/SBRouting/**

## Локальный просмотр

Для локальной разработки:
```bash
npm run dev
```

Для просмотра production версии локально:
```bash
npm run build
npm run preview
```
