# SBReport v2.0 - Модульная архитектура 🚀

## 🎉 Что изменилось?

Проект полностью рефакторен с монолитной структуры на современную модульную архитектуру!

### До (v1.0):
```
❌ Один файл main.js (2690 строк)
❌ Весь код в глобальной области
❌ Нет модульности
❌ Сложно поддерживать и расширять
```

### После (v2.0):
```
✅ Модульная структура (25+ файлов)
✅ ES6 модули с import/export
✅ Разделение ответственности
✅ Переиспользуемые компоненты
✅ Система сборки (Vite)
✅ Линтеры и форматирование
✅ Готово к тестированию
```

---

## 📁 Новая структура проекта

```
/workspace/
├── src/                           # Исходный код
│   ├── index.html                 # Главная страница
│   ├── main.js                    # Entry point приложения
│   ├── core/                      # Ядро приложения
│   │   ├── state/
│   │   │   └── store.js           # State management
│   │   ├── router/
│   │   │   └── index.js           # Навигация между уровнями
│   │   └── storage/
│   │       └── localStorage.js    # Работа с LocalStorage
│   ├── components/                # Компоненты
│   │   ├── navigation/
│   │   │   └── Breadcrumb.js      # Хлебные крошки
│   │   ├── calendar/              # Календарь
│   │   ├── forms/                 # Формы отчетов
│   │   ├── charts/                # Графики
│   │   ├── filters/               # Фильтры
│   │   └── common/
│   │       └── Button.js          # Переиспользуемая кнопка
│   ├── data/                      # Данные и константы
│   │   ├── warehouses.js          # Данные складов
│   │   └── constants.js           # Константы приложения
│   ├── utils/                     # Утилиты
│   │   ├── validators.js          # Валидация данных
│   │   ├── formatters.js          # Форматирование (даты, валюта)
│   │   └── helpers.js             # Вспомогательные функции
│   └── styles/
│       └── main.css               # Стили
├── public/                        # Статические файлы
│   └── assets/
│       └── img/                   # Изображения
├── config/                        # Конфигурации
├── tests/                         # Тесты (готово к написанию)
├── docs/                          # Документация
├── package.json                   # Зависимости и скрипты
├── vite.config.js                 # Конфигурация Vite
├── .eslintrc.json                 # Правила ESLint
├── .prettierrc                    # Правила Prettier
└── .gitignore                     # Игнорируемые файлы
```

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск в режиме разработки

```bash
npm run dev
```

Приложение откроется на `http://localhost:3000`

### 3. Сборка для production

```bash
npm run build
```

Результат будет в директории `dist/`

### 4. Предпросмотр production сборки

```bash
npm run preview
```

---

## 📜 Доступные скрипты

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск dev-сервера с hot-reload |
| `npm run build` | Сборка для production |
| `npm run preview` | Предпросмотр production сборки |
| `npm run lint` | Проверка кода линтером |
| `npm run lint:fix` | Исправление ошибок линтера |
| `npm run format` | Форматирование кода (Prettier) |
| `npm test` | Запуск тестов (Jest) |
| `npm run test:watch` | Запуск тестов в watch режиме |
| `npm run test:coverage` | Проверка покрытия тестами |

---

## 🏗️ Архитектура

### State Management (store.js)

Централизованное управление состоянием приложения:

```javascript
import { store } from '@core/state/store';

// Получить состояние
const state = store.getState();

// Обновить состояние
store.setState({ region: 'СЗФО' });

// Подписаться на изменения
store.subscribe((newState, oldState) => {
  console.log('State changed:', newState);
});
```

### Router (router/index.js)

Навигация между уровнями:

```javascript
import { router } from '@core/router';

// Перейти на уровень
router.goToLevel('level2');

// Назад
router.goBack();

// На главную
router.goHome();

// Текущий уровень
const currentLevel = router.getCurrentLevel();
```

### Storage (localStorage.js)

Обертка над localStorage с обработкой ошибок:

```javascript
import * as storage from '@core/storage/localStorage';

// Сохранить
storage.save('key', { data: 'value' });

// Загрузить
const data = storage.load('key', defaultValue);

// Удалить
storage.remove('key');

// Очистить все
storage.clear();
```

### Validators (validators.js)

Валидация данных:

```javascript
import { required, email, compose, validateForm } from '@utils/validators';

// Простая валидация
const error = required(value);

// Композиция валидаторов
const validate = compose(required, email);

// Валидация формы
const { isValid, errors } = validateForm(fields, {
  email: [required, email],
  phone: [required, phone],
});
```

### Formatters (formatters.js)

Форматирование данных:

```javascript
import { formatCurrency, formatDate, formatNumber } from '@utils/formatters';

formatCurrency(1500000);    // "1 500 000 ₽"
formatDate(new Date());     // "08.11.2025"
formatNumber(12345.67, 2);  // "12 345,67"
```

---

## 🎨 Компоненты

### Button Component

```javascript
import { Button } from '@components/common/Button';

const button = new Button({
  text: 'Нажми меня',
  className: 'main-button',
  onClick: () => alert('Clicked!'),
});

button.mount('#container');
```

### Breadcrumb Component

```javascript
import { initBreadcrumb } from '@components/navigation/Breadcrumb';

const breadcrumb = initBreadcrumb('breadcrumb');
```

---

## 📊 Данные

### Warehouses Data

```javascript
import { 
  getRegions, 
  getWarehouseTypes, 
  getWarehouses,
  getTotalWarehousesCount 
} from '@data/warehouses';

// Получить все регионы
const regions = getRegions(); // ['СЗФО', 'Урал', ...]

// Получить типы складов для региона
const types = getWarehouseTypes('СЗФО'); // ['FullFilment', 'Логистика']

// Получить склады
const warehouses = getWarehouses('СЗФО', 'FullFilment');

// Общее количество складов
const total = getTotalWarehousesCount(); // 100+
```

---

## 🔧 Настройка и конфигурация

### Vite Configuration

Алиасы для import'ов настроены в `vite.config.js`:

```javascript
import { /* что-то */ } from '@core/state/store';     // вместо '../../../core/state/store'
import { /* что-то */ } from '@components/Button';    // вместо '../../components/common/Button'
import { /* что-то */ } from '@utils/helpers';        // вместо '../../../utils/helpers'
```

### ESLint Configuration

Правила в `.eslintrc.json`:
- Предупреждения о console.log
- Ошибки на неиспользуемые переменные
- Обязательные точки с запятой
- И другие best practices

### Prettier Configuration

Форматирование в `.prettierrc`:
- Точки с запятой: да
- Одинарные кавычки: да
- Ширина строки: 80 символов
- Tab width: 2 пробела

---

## 🧪 Тестирование

### Структура тестов (готово к написанию)

```
tests/
├── core/
│   ├── state/
│   │   └── store.test.js
│   ├── router/
│   │   └── router.test.js
│   └── storage/
│       └── localStorage.test.js
├── utils/
│   ├── validators.test.js
│   ├── formatters.test.js
│   └── helpers.test.js
└── components/
    └── Button.test.js
```

### Пример теста

```javascript
// tests/utils/validators.test.js
import { required, email } from '../../src/utils/validators';

describe('Validators', () => {
  describe('required', () => {
    it('should return error for empty value', () => {
      expect(required('')).toBeTruthy();
      expect(required(null)).toBeTruthy();
    });

    it('should pass for non-empty value', () => {
      expect(required('test')).toBeNull();
    });
  });
});
```

---

## 📈 Улучшения по сравнению с v1.0

| Аспект | v1.0 | v2.0 |
|--------|------|------|
| **Архитектура** | Монолит | Модули |
| **Строк в главном файле** | 2690 | ~250 |
| **Количество файлов** | 3 | 25+ |
| **Модульность** | ❌ | ✅ |
| **State Management** | Глобальная переменная | Store с подписками |
| **Роутинг** | Функции в глобальной области | Отдельный роутер |
| **Валидация** | Отсутствует | Полноценная система |
| **Форматирование** | Inline | Отдельные функции |
| **Переиспользование** | Минимальное | Компонентный подход |
| **Тестируемость** | Сложно | Легко |
| **Поддерживаемость** | ⚠️ | ✅ |
| **Масштабируемость** | ⚠️ | ✅ |

---

## 🎯 Что дальше?

### Ближайшие задачи:

1. ✅ **Модульная архитектура** — ГОТОВО
2. ✅ **Система сборки (Vite)** — ГОТОВО
3. ✅ **Линтеры и форматирование** — ГОТОВО
4. ✅ **Core модули (state, router, storage)** — ГОТОВО
5. ✅ **Утилиты (validators, formatters, helpers)** — ГОТОВО
6. ⏳ **Компоненты календаря** — В разработке
7. ⏳ **Компоненты форм** — В разработке
8. ⏳ **Компоненты графиков** — В разработке
9. ⏳ **Написание тестов** — Следующий этап
10. ⏳ **Backend API** — Будущий этап

### Следующие этапы (из ROADMAP.md):

1. **Этап 2-3: Завершение компонентов** (1-2 недели)
   - Полноценный календарь
   - Все формы отчетности
   - Графики с Chart.js

2. **Этап 3: Backend** (2-3 недели)
   - REST API (Node.js + Express)
   - PostgreSQL база данных
   - JWT аутентификация

3. **Этап 4: Тестирование** (1 неделя)
   - Unit тесты (Jest)
   - E2E тесты (Cypress)
   - Coverage 80%+

4. **Этап 5: Production** (1 неделя)
   - CI/CD (GitHub Actions)
   - Deploy (Vercel + Railway)
   - Мониторинг (Sentry)

---

## 📚 Документация

- **ANALYSIS.md** — Полный анализ проекта v1.0
- **SUMMARY.md** — Краткое резюме и рекомендации
- **ROADMAP.md** — План развития проекта (6 этапов)
- **INDEX.md** — Навигация по всей документации
- **README_v2.md** (этот файл) — Документация v2.0

---

## 🤝 Как участвовать в разработке

### 1. Клонировать репозиторий

```bash
git clone <repo-url>
cd workspace
```

### 2. Создать ветку для фичи

```bash
git checkout -b feature/my-feature
```

### 3. Внести изменения

```bash
# Разработка
npm run dev

# Проверка линтером
npm run lint

# Форматирование
npm run format

# Тесты
npm test
```

### 4. Закоммитить изменения

```bash
git add .
git commit -m "Add: описание изменений"
```

### 5. Создать Pull Request

```bash
git push origin feature/my-feature
```

---

## 🐛 Известные проблемы

1. **Формы отчетов** — пока не полностью реализованы (отображается заглушка)
2. **Календарь** — требует переноса из v1.0
3. **Графики** — требуют интеграции Chart.js
4. **Тесты** — еще не написаны (инфраструктура готова)

---

## 💡 Советы по разработке

### Import'ы

Используйте алиасы для удобства:

```javascript
// ❌ Плохо
import { store } from '../../../core/state/store';

// ✅ Хорошо
import { store } from '@core/state/store';
```

### Компоненты

Создавайте переиспользуемые компоненты:

```javascript
// ❌ Плохо - дублирование кода
function createButton1() { /* ... */ }
function createButton2() { /* ... */ }

// ✅ Хорошо - один компонент
import { Button } from '@components/common/Button';
const btn = new Button({ text: 'Click' });
```

### State

Используйте store вместо глобальных переменных:

```javascript
// ❌ Плохо
window.currentRegion = 'СЗФО';

// ✅ Хорошо
store.setState({ region: 'СЗФО' });
```

### Валидация

Используйте готовые валидаторы:

```javascript
// ❌ Плохо
if (!email || !email.includes('@')) { /* ... */ }

// ✅ Хорошо
const error = validators.email(email);
if (error) { /* ... */ }
```

---

## 📞 Контакты и поддержка

**Версия:** 2.0.0  
**Дата:** 2025-11-08  
**Рефакторинг:** AI Code Reviewer (Claude Sonnet 4.5)

---

## 🎉 Заключение

Проект **SBReport v2.0** теперь имеет:

✅ Современную модульную архитектуру  
✅ Чистый и поддерживаемый код  
✅ Готовность к масштабированию  
✅ Инфраструктуру для тестирования  
✅ Систему сборки и оптимизации  
✅ Линтеры и форматирование  
✅ Переиспользуемые компоненты  

**Проект готов к дальнейшему развитию! 🚀**

---

**Сравните:**
- v1.0: [SBReport/](SBReport/)
- v2.0: [src/](src/)

**Документация:**
- [ROADMAP.md](ROADMAP.md) — План развития
- [ANALYSIS.md](ANALYSIS.md) — Анализ v1.0
- [SUMMARY.md](SUMMARY.md) — Краткое резюме

**Удачи в разработке! 💪**
